# Lovable prompt: finish invoice creation and test the payment path

Use merged GitHub PR #77 plus follow-up PR #78 as the source of truth. Review PR #78's latest commit before making changes. The app's preview and live URLs currently share a backend with real client data, so first set up an isolated test backend with synthetic clients. Do not test invoice creation on real clients or register live Stripe or SignWell webhooks. Do not overwrite the PR's Documents library, item picker, client invoice form, invoice preview, or contract picker.

The current failure happens after selecting priced items: the UI calculates the total, but saving the invoice returns a generic failure. Verify the actual Supabase error. Confirm whether `public.client_invoices.line_items` and `published_at` exist in the test backend and whether the authenticated user has the `dream_wave_owner` role. Apply the additive migration `supabase/migrations/20260915014000_invoice_line_items.sql` to the isolated test backend. Reconcile it with the existing Drizzle migrations `0000_documents_center_foundation.sql`, `0001_provider_webhooks.sql`, and `0002_invoice_line_items.sql`; apply each needed migration once and check for existing objects before executing it. Never apply this migration to the shared live backend during this test.

In Documents, an invoice item template needs an item title, full description, quantity, and USD unit price. In the client's invoice form, the picker should show only item titles and provide a search bar. Choosing a title should copy the full description, quantity, unit price, source template ID/version, and title into that client's invoice. Allow multiple items and multiple separate invoices for the same client. Generate a unique invoice number with `next_invoice_number` when the owner leaves the number blank. The amount must equal the sum of quantity × unit price, calculated and checked server-side or in a database constraint. Saving must create a persistent `client_invoices` row for the selected workspace, return its ID, close the form, and refresh the admin invoice list. Editing or removing items must update that row and keep the total in sync. Do not silently discard item details or write the invoice to another client.

Show each saved invoice's title, description, quantity, unit price, and line total in the client invoice card and in the branded invoice print/preview. Verify the existing Dream Wave Media logo and business profile appear. A private draft must be visible only to the owner. Publishing to the portal must be an explicit action; do not claim an email was sent unless the email integration actually succeeds. Show the provider or database error in a useful safe message if saving fails, and keep the unsaved form and its items intact.

Test in the isolated backend with synthetic clients: create two invoices for one client, one with two item templates and one with one item; refresh and confirm both rows and their item snapshots; create an invoice for a second client and verify no cross-client access; edit a price and remove an item; verify totals, preview, and portal display; verify a non-owner cannot create or see private drafts. The expected result is that invoice creation works without any Stripe secret or webhook. Stripe is only needed to collect and confirm payments. After invoice creation passes, test a Stripe test-mode checkout and webhook separately using test credentials and a test endpoint, including delayed payment success/failure. Do not enable live charges or automatic delivery unlocking on the shared backend.

Report the database objects applied, the exact invoice creation error if it persists, test evidence, and any remaining Stripe setup. Leave production migration, live provider registration, emails, and deployment for a separately approved release.
