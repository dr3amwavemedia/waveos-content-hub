# Paste this into Lovable after reviewing the GitHub PRs

Continue the WaveOS Documents, contracts, invoices, and payments work using the GitHub changes in PR #79 (`codex/documents-contract-builder`) and PR #80 (`codex/payment-ledger`). First sync the merged code from GitHub. Do not recreate its screens or overwrite existing client records. Preserve the selected client's workspace ID on every invoice, contract, form, receipt, and linked Bloom ledger record. If an invoice number matches more than one client, leave the Bloom record unassigned for owner review.

The current Lovable preview and live app share one backend with real client data. Before applying either PR's additive migration or registering provider webhooks, create a genuinely separate test backend and confirm the preview uses it. If that backend is still shared, review and build code only; do not apply database changes, deploy a webhook receiver, send mail or SignWell requests, make a real charge, or unlock real deliverables. Do not treat a Stripe test key as a substitute for database isolation.

In the isolated test backend, apply and verify the contract variable snapshot migration from PR #79, then `20260915153348_payment_ledger.sql` from PR #80. Verify owner-only row-level security on `payment_ledger`: clients and staff cannot read the books, owners can import Bloom CSV rows and change only their review status, and Stripe webhook writes run server-side. Confirm invoice and contract visibility remains client-specific, drafts are private, and archived or versioned templates cannot change older documents. Use fake clients, fake invoice line items, and fake CSV exports for checks.

Use the Documents page and client contract builder from PR #79. Contracts should allow editable client name, business name, services purchased, project date, today's date, location, and project name, with `{{client_name}}`, `{{business_name}}`, `{{services}}`, `{{project_date}}`, `{{today_date}}`, `{{location}}`, and `{{project_name}}` resolving from the selected client's current profile/project/invoice details. Show unresolved values before saving. Save a snapshot of the values and rendered text to the assigned client's private contract draft; selecting another client never moves it. Keep templates searchable and reusable, and retain the invoice-item picker with title-only search results plus correctly copied descriptions, quantities, prices, and totals on the finished invoice.

Use the existing selected Dream Wave Media logo asset from `businessProfile` on invoice templates and exported invoices, with `dwmsrq.com`, `Sarasota, FL`, and `(941) 294-5727`. Do not invent a street address, business e-mail, legal entity name, tax ID, or payment instructions; the current profile leaves those fields unset until verified. Store document files privately, authorize every read and signed download for the assigned client or owner, use short-lived links, and keep document contents and secrets out of notification e-mails and logs.

Use the Payments owner page from PR #80. Its day, week, month, and year view is for dated cash receipts, with refunds separate, a net-received amount, an invoice collection progress bar, and a dated trend. Bloom invoice imports are billed amounts and do not become earnings. Bloom CSV imports must map source ID, amount, and date, show a preview, reject malformed files, and skip already-imported source IDs. Link imported records to a client invoice only when the invoice number has exactly one match. Bloom payments and refunds begin pending and must be checked for overlap with Stripe before the owner posts them. Stripe mismatched payments and refund events also need owner review. Posting a ledger row must never change invoice payment status or delivery access. Existing invoices with a reliable `paid_at` date are backfilled; older partial manual payments without a payment date cannot appear in day-based totals. Show this limitation plainly.

Diagnose the reported Stripe Checkout page that remains on a white skeleton after clicking Pay. The screenshot alone does not identify the cause. In test mode, create a fresh session from a fake sent invoice and inspect its Stripe Dashboard session ID, open/expired status, test/live flag, amount, currency, and hosted URL. Lovable owns the automatically created `STRIPE_SECRET_KEY`, so add a separate user-managed secure server secret named `WAVEOS_STRIPE_TEST_SECRET_KEY` containing an `sk_test_` key. The custom invoice Checkout code must use that secret and return an `https://checkout.stripe.com` URL. Test the hosted URL in a clean browser profile and inspect browser console/network errors if it still stalls. Report the actual failing request or provider error before changing more code. PR #80 already expires an old open session on retry, uses a new idempotency key per attempt, protects concurrent session claims, times out Stripe API requests, and rejects non-test or invalid destinations. Do not expose keys in chat, logs, CSV files, or this prompt.

Only after the isolated backend works, configure Stripe's test-mode event destination for the staging webhook endpoint (`/api/public/hooks/stripe`) under **Your account** unless this project truly uses Stripe Connect. Subscribe to `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `checkout.session.async_payment_failed`, `checkout.session.expired`, `charge.refunded`, and `charge.dispute.created`. Put that destination's `whsec_...` signing secret into the secure server secret `STRIPE_WEBHOOK_SECRET`; it is separate from `WAVEOS_STRIPE_TEST_SECRET_KEY`. Verify event signatures and test-mode flags, deduplicate payments, mark a paid invoice and release a payment hold only for the correct session, expected balance, and settled payment status. Leave refunds and disputes flagged for owner review; do not revoke access automatically. Configure the staging SignWell webhook (`/api/public/hooks/signwell`) and its secret separately; confirm the registered webhook ID is used for signature verification and only `document_completed` finishes a contract. No real client e-mail or signature request should be sent during these tests.

Run through: create two fake clients; make invoice item templates with descriptions and USD prices; create one multi-item invoice for the correct client; save it as a draft, then send it in test mode; create a contract from a variable template for that same client; complete one Stripe test payment and confirm the ledger, invoice status, and automatic delivery hold release; try delayed/expired checkout and verify nothing unlocks; import a fake Bloom CSV twice and verify the second pass skips duplicates; verify only posted payments affect period totals. Give me a concise list of what passed, what failed, the actual Stripe loading cause if found, and any backend migration or secret I still need to set up. Do not switch to live charging until I expressly ask.
